import React from 'react'; 
import './App.css'; 
import { useWeather } from "./hooks";

function App() {
  const { } = useWeather();
  
  return (
    <div className="App">
      <header className="App-header">  
      </header>
    </div>
  );
}

export default App;
